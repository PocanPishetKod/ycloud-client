import { HttpResponse, HttpResponseBase } from "@angular/common/http";
import { Injectable } from "@angular/core";

@Injectable({
    providedIn: "root"
})
export class StatusCodesHandlerService {
    public handle(httpResponse: HttpResponseBase): void {
        
    }
}